# Generate with: openssl rand -base64 32
SECRET_KEY=

# Generate with: openssl rand -base64 32
AUTH_SECRET=

# Set to your Ubuntu machine's local IP, e.g. http://192.168.1.50:3000
# Find it with: ip addr show | grep "inet " | grep -v 127
AUTH_URL=http://UBUNTU_IP:3000
